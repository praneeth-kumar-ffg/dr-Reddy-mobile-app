	CREATE TABLE “SCHEMA_NAME"."TBL_JOB_INFORMATION"

(	"JOB_ID" NUMBER(20,0) NOT NULL ENABLE,
	“COMPANY_NAME" VARCHAR2(50 BYTE) NOT NULL ENABLE,
	“DESIGNATION" VARCHAR2(50 BYTE) NOT NULL ENABLE,
	“JOB_DESCRIPTION” VARCHAR2(1000 BYTE),
	"CITY_ID" NUMBER(20,0),
	"VACANCY_COUNT" NUMBER(20,0),
	“QUALIFICATION_REQ” VARCHAR2(1000 BYTE),
	“CREATE_TIMESTAMP” TIMESTAMP(6),
	CONSTRAINT “TBL_JOB_INFORMATION_PK” PRIMARY KEY( “JOB_ID")
)
	CREATE TABLE ”SCHEMA_NAME"."TBL_JOB_APPLICATION_STATUS"

(	“STUDENT_ID" NUMBER(20,0) NOT NULL ENABLE,
	“JOB_ID" NUMBER (20,0) NOT NULL ENABLE,
	“APPLICATION_STATUS“ VARCHAR2(20 BYTE),
	“DATE_OF_SELECTION" DATE,
	“TIMESTAMP" TIMESTAMP(6),
	CONSTRAINT “TBL _JOB APPLICATION STATU_FK1” FOREIGN KEY (“STUDENT_ID")
	REFERENCES “SCHEMA_NAME”."TBL_PROFILE_DATA" (“STUDENT_ID”) ENABLE,
	CONSTRAINT “TBL_JOB_APPLICATION_STATU_FK2" FOREIGN KEY (“JOB_ID”)
	REFERENCES “SCHEMA_NAME"."TBL_JOB_INFORMATION" (“JOB_ID”) ENABLE
)
	CREATE TABLE ”SCHEMA_NAME"."TBL_CITY_DETAILS"

(	"CITY_ID" NUMBER(20,0) NOT NULL ENABLE,
	“CITY" VARCHAR2(20 BYTE) NOT NULL ENABLE,
	"STATE" VARCHAR2(20 BYTE) NOT NULL ENABLE,
	“CREATE_TIMESTAMP" TIMESTAMP(6),
	CONSTRAINT “TBL_CITY_DETAILS_PK” PRIMARY KEY(“CITY_ID")
)
	CREATE TABLE "SCHEMA_NAME"."TBL_PROFILE_DATA"
(	“STUDENT_ID" NUMBER(20,0) NOT NULL ENABLE,
	“FIRST_NAME" VARCHAR2(30 BYTE),
	“LAST_NAME" VARCHAR2(30 BYTE),
	“MOBILE_NUMBER" NUMBER(20,0),
	"EMAIL _ADDRESS” VARCHAR2(30 BYTE),
	"ADDRESS“ VARCHAR2(200 BYTE),
	"CITY_ID" NUMBER(20,0),
	"CENTRE_ID" VARCHAR2(20 BYTE),
	“DATE_OF_BIRTH" DATE,
	“CREATE_TIMESTAMP" TIMESTAMP (6),
	“UPDATE_TIMESTAMP” TIMESTAMP (6),
	CONSTRAINT “TBL_PROFILE_DATA_PK" PRIMARY KEY (“STUDENT_ID”)
	CONSTRAINT “TBL_PROFILE_DATA_FK1” FOREIGN KEY (“CITY_ID")
	REFERENCES ”SCHEMA_NAME”.”TBL_CITY_DETAILS" (“CITY_ID”) ENABLE
)
	CREATE TABLE "SCHEMA_NAME"."TBL_ENGAGEMENT"
(	“ALERT_ID" VARCHAR2(20 BYTE) NOT NULL ENABLE,
	“ALERT_TYPE” VARCHAR2(200 BYTE),
	“ALERT_DESCRIPTION” VARCHAR2(1000 BYTE),
	“ACTIVE_STATUS” VARCHAR2(20 BYTE),
	“CREATE_TIMESTAMP” TIMESTAMP (6),
	“UPDATE_TIMESTAMP" TIMESTAMP (6),
	CONSTRAINT “TBL_ENGAGEMENT_PK PRIMARY KEY” (“ALERT_ID”)
)

	CREATE TABLE "SCHEMA_NAME"."TBL_WORK_EXPERIENCE"
(	“STUDENT_ID" NUMBER(20,0) NOT NULL ENABLE,
	"COMPANY_NAME" VARCHAR2(50 BYTE) NOT NULL ENABLE,
	"JOB_TITLE" VARCHAR2(30 BYTE) NOT NULL ENABLE,
	“FROM_DATE" DATE NOT NULL ENABLE,
	"TO_DATE" DATE,
	"CREATE_TIMESTAMP" TIMESTAMP (6),
	CONSTRAINT "TBL_WORK_EXPERIENCE_FKI" FOREIGN KEY("STUDENT_ID")
	REFERENCES “SCHEMA_NAME”."TBL_PROFILE_DATA"(STUDENT_ID") ENABLE
)
	CREATE TABLE "SCHEMA_NAME"."TBL_LOGIN_DETAILS"
(	”STUDENT_ID” NUMBER(20,0) NOT NULL ENABLE,
	"USERNAME" VARCHAR2(30 BYTE) NOT NULL ENABLE,
	“PASSWORD" VARCHAR2(30 BYTE) NOT NULL ENABLE,
	“CREATE_TIMESTAMP” TIMESTAMP (6),
	“UPDATE_TIMESTAMP" TIMESTAMP (6),
	CONSTRAINT “TBL_LOGIN_DETAILS_FK1" FOREIGN KEY("STUDENT_ID")
	REFERENCES "SCHEMA _NAME”.”TBL PROFILE_DATA” (“STUDENT_ID”) ENABLE
)
	CREATE TABLE "SCHEMA_NAME"."TBL_HELP_HISTORY"
(	“STUDENT_ID” NUMBER(20,0) NOT NULL ENABLE,
	“REASON" VARCHAR2(200 BYTE),
	“DETAILS" VARCHAR2(1000 BYTE),
	“CENTRE_ID" VARCHAR2(20 BYTE),
	“CREATE_TIMESTAMP" TIMESTAMP (6),
	CONSTRAINT “TBL_HELP_HISTORY_FK1” FOREIGN KEY (“STUDENT_ID")
	REFERENCES “SCHEMA_NAME”.”TBL_PROFILE DATA” (“STUDENT_ID") ENABLE
)

CREATE TABLE "SCHEMA_NAME"."TBL_ADMIN_HELP_REQUEST_STATUS"
(	"STUDENT_ID" NUMBER(20,0) NOT NULL ENABLE,
	"STATUS" VARCHAR2(20) NOT NULL,
	"CREATE_TIMESTAMP" TIMESTAMP(6),
	CONSTRAINT “TBL_ADMIN_HELP_REQUEST_STATUS_PK PRIMARY KEY” ("STUDENT_ID")
	CONSTRAINT “TBL_ADMIN_HELP_REQUEST_STATUS_FK1” FOREIGN KEY (“STUDENT_ID")
	REFERENCES “SCHEMA_NAME”.”TBL_PROFILE DATA” (“STUDENT_ID") ENABLE
);
alter table alumniconnect.tbl_job_information modify column  JOB_ID INT auto_increment;

CREATE TABLE ”SCHEMA_NAME"."TBL_HELP_DETAILS"
( "HELP_ID" NUMBER(20,0) NOT NULL ENABLE,
  "HELP_TYPE" VARCHAR2(20 BYTE) NOT NULL ENABLE,
  "HELP_VALUE" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
  "CREATE_TIMESTAMP" TIMESTAMP(6)
);
